
### Requirements installation
[`convert_manuscript.py`](convert_manuscript.py) requires library `MarkItDown` with its optional dependency `[pdf]`

```bash
mkdir markitdown-deps
pip install -r requirements.txt -t ./markitdown-deps
```